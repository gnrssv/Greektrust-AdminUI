import React from "react";
import {Button} from '@mui/material';

function Main(){
    return (
        <Button variant = "contained" color = "primary">
            Delete
        </Button>
    );
};

export default Main;